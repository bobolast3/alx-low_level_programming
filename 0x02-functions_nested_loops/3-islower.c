#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
/**
 * Description: 'the program's description'
 * @parameter: c something
 *
 * Return: Always 1 (Success)
 */
int _islower(int c)
{
if (c >= 97 && c <= 122)
{
return (1);
}
return (0);
}
